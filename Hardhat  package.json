{
  "name": "adam-protocol",
  "version": "1.0.0",
  "description": "ADAM Protocol v6.0 – Civilization OS (ZK-Proof-of-Action on Qubic/EVM)",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:qubic": "hardhat run scripts/deploy.js --network qubic",
    "verify:qubic": "hardhat run scripts/verify.js --network qubic"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "chai": "^4.4.1",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  }
}
