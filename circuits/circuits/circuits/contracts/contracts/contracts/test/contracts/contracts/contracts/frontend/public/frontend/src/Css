/* =========================================================================
   ADAM · Campus Genesis UI Theme
   Black · Gold · Celestial Mesh Aesthetic
   ========================================================================= */

:root {
  --bg-black: #000000;
  --bg-panel: #0a0a0f;
  --gold: #d4a637;
  --gold-soft: #e7c46a;
  --white: #f7f7f7;
  --gray: #7a7a7a;

  --card-bg: #111118;
  --card-border: #1f1f28;

  --radius: 14px;
  --shadow: 0 0 25px rgba(255, 215, 100, 0.08);
  --glow: 0 0 12px rgba(212, 166, 55, 0.35);
}

/* =========================================================================
   GLOBAL
   ========================================================================= */

body {
  margin: 0;
  padding: 0;
  font-family: "Inter", "Roboto", sans-serif;
  background-color: var(--bg-black);
  color: var(--white);
  -webkit-font-smoothing: antialiased;
}

.celestial-container {
  max-width: 1180px;
  margin: auto;
  padding: 20px;
}

/* =========================================================================
   HEADER
   ========================================================================= */

.celestial-header {
  text-align: center;
  margin-bottom: 35px;
}

.gold-title {
  font-size: 2.4rem;
  font-weight: 700;
  letter-spacing: 1px;
  color: var(--gold);
  text-shadow: var(--glow);
  margin: 0;
}

.gold-subtitle {
  font-size: 1rem;
  margin-top: 6px;
  color: var(--gold-soft);
  opacity: 0.85;
}

/* =========================================================================
   GRID LAYOUT
   ========================================================================= */

.dashboard-grid {
  display: grid;
  gap: 22px;
  grid-template-columns: 2fr 1fr;
}

.main-dashboard {
  background: var(--card-bg);
  padding: 22px;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.control-panel {
  background: var(--card-bg);
  padding: 20px;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.leaderboard-section {
  grid-column: span 2;
  background: var(--card-bg);
  padding: 20px;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

/* =========================================================================
   METRIC CARDS
   ========================================================================= */

.metric-card {
  background: #121218;
  border: 1px solid var(--card-border);
  padding: 18px;
  border-radius: var(--radius);
  margin-bottom: 15px;
  transition: 0.25s ease;
}

.metric-card:hover {
  border-color: var(--gold);
  box-shadow: var(--glow);
}

.metric-value {
  font-size: 1.9rem;
  font-weight: 600;
  color: var(--gold);
}

.metric-label {
  font-size: 0.9rem;
  margin-top: 5px;
  color: var(--gray);
}

/* =========================================================================
   PROOF GENERATOR PANEL
   ========================================================================= */

.proof-generator {
  margin-bottom: 28px;
}

.proof-generator h2 {
  font-size: 1.2rem;
  color: var(--gold-soft);
  margin-bottom: 14px;
}

.proof-generator label {
  display: block;
  margin-bottom: 12px;
  font-size: 0.9rem;
  color: var(--white);
}

.proof-generator input {
  width: 100%;
  padding: 12px;
  margin-top: 4px;
  border-radius: var(--radius);
  border: 1px solid var(--card-border);
  background: #0c0c12;
  color: var(--white);
  font-size: 1rem;
  transition: border 0.2s ease;
}

.proof-generator input:focus {
  outline: none;
  border-color: var(--gold);
  box-shadow: var(--glow);
}

.generate-button {
  width: 100%;
  padding: 14px;
  background: var(--gold);
  border: none;
  border-radius: var(--radius);
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  margin-top: 6px;
  color: #000;
  transition: 0.25s;
}

.generate-button:hover {
  background: var(--gold-soft);
  box-shadow: var(--glow);
}

/* =========================================================================
   BLQ TRACKER
   ========================================================================= */

.blq-tracker h2 {
  font-size: 1.2rem;
  color: var(--gold);
  margin-bottom: 10px;
}

.blq-balance {
  font-size: 2.2rem;
  font-weight: 700;
  color: var(--gold);
  margin-bottom: 10px;
}

/* =========================================================================
   LEADERBOARD
   ========================================================================= */

.leaderboard h2 {
  color: var(--gold-soft);
  margin-bottom: 16px;
  font-size: 1.3rem;
}

.leaderboard-entry {
  display: flex;
  justify-content: space-between;
  padding: 12px 15px;
  margin-bottom: 8px;
  background: #111118;
  border: 1px solid var(--card-border);
  border-radius: var(--radius);
  transition: 0.25s;
}

.leaderboard-entry:hover {
  border-color: var(--gold);
  box-shadow: var(--glow);
}

.rank {
  color: var(--gold);
  font-weight: 700;
}

/* =========================================================================
   FOOTER
   ========================================================================= */

.celestial-footer {
  margin-top: 40px;
  padding: 22px;
  text-align: center;
  color: var(--gray);
  font-size: 0.9rem;
  border-top: 1px solid #1d1d1d;
}

.footer-note {
  margin-top: 6px;
  color: var(--gold-soft);
}

/* =========================================================================
   RESPONSIVE
   ========================================================================= */

@media (max-width: 900px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
  }

  .leaderboard-section {
    grid-column: span 1;
  }
}

@media (max-width: 600px) {
  .gold-title {
    font-size: 1.8rem;
  }

  .metric-value {
    font-size: 1.5rem;
  }

  .generate-button {
    font-size: 0.95rem;
    padding: 12px;
  }
}
